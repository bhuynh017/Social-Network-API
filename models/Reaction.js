// requriing two modules from the mongoose library.
const { Schema, Types } = require('mongoose');
// calling the utils dateFormat.js
const dateFormat = require('../utils/dateFormat');

const reactionSchema = new Schema(
    
);